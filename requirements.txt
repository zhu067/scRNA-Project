# 核心（与 pyproject 一致）。安装： pip install -e ".[full]" 或：
#   pip install -r requirements.txt && pip install liana decoupler gseapy pybiomart

anndata>=0.10
scanpy>=1.9
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
mygene>=3.2
requests>=2.28
matplotlib>=3.7

# 可选：线粒体基因 Biomart、双细胞
# pybiomart
# scrublet

# 可选：PEC 肾炎通讯、GO
# liana>=1.5
# decoupler>=2.0
# gseapy>=1.0
